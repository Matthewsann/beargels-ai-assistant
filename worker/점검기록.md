# 새벽 자동 점검 기록

매일 03:30 집 PC가 어제 오류를 점검한 결과. 최신이 맨 위.

---

## 2026-08-07

- 오류 0건 — 이상 없음(고칠 것 없음)
- 고친 것: 없음
- 배운 규칙: 없음 — 수정 쌍 1건뿐(쿠팡 질문형, "가장 많이 사랑받는" 문구·느낌표·이모지 추가)이라
  반복 패턴 아님 → 규칙 추가 보류
- 유형별 수정률: question 1건 중 1건 수정(100%) — 표본 1건이라 참고용
- **사장님이 해야 할 것**: 없음

---

## 2026-08-06

- 오류 0건 — 이상 없음(고칠 것 없음)
- 고친 것: 없음
- 배운 규칙: 없음 — 최근 2일간 직원이 고친 답글 쌍이 없음(등록된 리뷰 자체가 없거나 원본 그대로 등록됨)
- 유형별 수정률: 집계 대상 없음(`posted` + `ai_draft` 있는 리뷰 0건) — 스키마 오류는 아님(쿼리 정상 동작 확인)
- **사장님이 해야 할 것**: 없음. 리뷰 답글이 아직 등록되지 않고 있다면(직원이 '답글 등록함'을
  안 눌렀거나 리뷰가 없었던 것) 확인만 해주면 된다.

---

## 2026-08-05

- 오류 5건(전부 같은 원인: `/menu` 500 TemplateNotFound, 7/29 발생분) → 5건 모두 닫음
- 고친 것: 원인은 7/29 `cf8e8f7`(deploy.yml 업로드 목록에 menu.html 누락)로 이미 해결됨.
  같은 실수 재발 방지용 회귀 테스트 `tests/test_deploy_templates.py` 추가 (전체 52건 통과)
- 배운 규칙: 없음 — 어제 직원이 고친 답글 쌍을 읽지 못했다(아래 참고)
- 유형별 수정률: 집계 불가 — `reviews` 에 `ai_draft`/`kind`/`posted_at` 컬럼이 아직 없음
- **사장님이 해야 할 것**
  1. Supabase SQL Editor 에서 `database/schema_v5.sql` 실행 → 이거 전까지
     '답글 등록함' 버튼과 수정률 집계가 계속 실패함
  2. PythonAnywhere 는 손댈 것 없음(이번엔 웹앱 코드 변경 없음)
