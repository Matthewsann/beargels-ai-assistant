# 베어글스 SNS 봇 — 시작하기 (윈도우, 집 PC)

이 폴더를 바탕화면에 두고, 아래 순서대로 하면 됩니다.
파란 글씨 명령어는 안 쳐도 되고, **번호 붙은 파일을 더블클릭**하면 됩니다.

---

## 준비물 (미리 모아두기)

`.env` 파일에 넣을 값들입니다. 아직 없는 건 아래 위치에서 복사해 오세요.

| 값 | 어디서 | 비고 |
|---|---|---|
| `ANTHROPIC_API_KEY` | https://console.anthropic.com → API Keys | Claude 캡션 생성용 |
| `TELEGRAM_BOT_TOKEN` | 기존 베어글스 비서 봇과 동일 | @BotFather |
| `TELEGRAM_CHAT_ID` | 기존 비서와 동일 (Matthew와의 채팅방 ID) | |
| `SUPABASE_URL` / `SUPABASE_KEY` | Supabase → Settings → API | 기존 비서 프로젝트 |
| `GOOGLE_DRIVE_FOLDER_ID` | 이미 채워둠 (오픈후 폴더) | 바꿀 일 없으면 그대로 |
| `BUFFER_ACCESS_TOKEN` | https://buffer.com/developers/apps | |
| `INSTAGRAM_PROFILE_ID` | `get_instagram_id.bat` 더블클릭하면 자동으로 알려줌 | Buffer 토큰 먼저 채운 뒤 |
| `client_secret.json` | 구글 클라우드 OAuth 클라이언트(데스크톱) | 파일을 이 폴더에 저장 |

---

## 순서

### 1. 설치 — `1_setup.bat` 더블클릭
- 필요한 부품을 자동 설치하고, `.env` 파일을 만들어 줍니다.
- 검은 창에 "Setup finished"가 뜨면 닫으세요.

### 2. `.env` 값 채우기
- 이 폴더의 **`.env`** 파일을 **메모장**으로 엽니다. (우클릭 → 연결 프로그램 → 메모장)
- 각 줄의 `=` 뒤에 위 표의 값을 붙여넣고 저장(Ctrl+S).
- 예: `ANTHROPIC_API_KEY=sk-ant-...`

### 3. `client_secret.json` 넣기
- 구글 클라우드에서 받은 OAuth 클라이언트 JSON 파일을
  **`client_secret.json`** 이름으로 이 폴더에 저장.

### 4. 점검 — `2_check.bat` 더블클릭
- 값이 잘 들어갔는지, 텔레그램·Supabase 연결이 되는지 ✅/❌로 알려줍니다.
- ❌ 가 있으면 그 항목을 고친 뒤 다시 실행.

### 5. 구글 로그인 — `3_google_login.bat` 더블클릭
- 브라우저가 열리면 **사진 폴더가 있는 구글 계정**으로 로그인 → 허용.
- "확인되지 않은 앱" 경고 → 고급 → 이동 (본인 앱이라 안전).
- 완료되면 `token.json`이 생깁니다. (한 번만 하면 됨)

### 6. 봇 실행 — `4_run_bot.bat` 더블클릭
- 검은 창이 열린 채로 두면 봇이 계속 돕니다. (창을 닫으면 봇도 멈춤)
- 이제 오픈후 폴더에 사진을 올리면 몇 분 안에 텔레그램으로 승인 요청이 옵니다.
- 24시간 돌리려면 이 창을 계속 켜두거나, PC 시작 시 자동 실행되게 하세요 (필요하면 안내해 드립니다).

---

## 문제가 생기면
- `2_check.bat`을 돌려서 나온 ✅/❌ 화면을 캡처해서 물어보세요.
- 대부분 `.env` 값 오타이거나, 파일 이름이 다른 경우입니다.
