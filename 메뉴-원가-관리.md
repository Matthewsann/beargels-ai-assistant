# 메뉴 정본 · 원가/마진 관리 (2026-07 구축)

채널(배민·쿠팡이츠·네이버·매장)마다 메뉴 정보가 제각각이고 원가계산이 깨져
있어서, **직원용 웹의 `/menu` 화면을 단일 정본**으로 만들었다.
여기 값이 기준이고, 각 채널 포털에는 이 기준을 보고 맞춘다.

## 처음 켜는 법 (1회)

1. Supabase SQL Editor에서 `supabase/migrations/005_menu_master.sql` 실행 (테이블 생성)
2. 집 PC에서 `python scripts/seed_menu_master.py` 실행 (초기 데이터 주입)
3. 직원용 웹 주소 뒤에 `/menu` 붙여서 열기 → `https://.../<비밀주소>/menu`
   (리뷰 답글 화면 오른쪽 위 "🥯 메뉴/원가" 링크로도 이동)

시드를 다시 만들어 전부 덮어쓰려면 `--force` (웹에서 고친 값도 덮어씀, 주의).

## 화면에서 할 수 있는 것

- **메뉴 정보 확인/수정**: 메뉴명·매장가·배달가·재료원가 — 셀 탭하면 바로 수정(자동 저장)
- **원가율**: 매장/배달 각각. 카테고리별 목표(🎯 버튼) 초과 시 빨간색
- **마진**: 채널별(매장·배민·쿠팡·네이버) = 판매가 − 중개수수료 − 결제수수료
  − 수수료 부가세(10%) − 가게부담 배달비 − 재료원가. 역마진은 빨간 숫자
- **채널 예외**: 메뉴를 탭하면 채널별 메뉴명/가격/판매여부 예외 설정
  (예외가 없으면 정본 그대로 = 통일된 상태)
- **수수료 설정**(⚙): 채널별 중개%·결제%·가게부담 배달비
- **"문제만" 체크**: 원가 미입력·목표 초과·역마진 메뉴만 필터

## 신메뉴 추가 (2026-08-17)

`/menu` 왼쪽 위 **➕ 새 메뉴**. 이름과 분류만 넣으면 되고, **SKU 는 분류에 맞춰
자동으로 붙는다**(디저트 → `DESRT-039`). 만들고 나면 상세 창이 바로 열리니
거기서 원가·소개글을 이어서 채우면 된다.

| | |
|---|---|
| 분류별 접두사 | 베이커리 `BK` · 크림치즈 `CREAM` · 샌드위치 `SAND` · 샐러드 `SALAD` · 디저트 `DESRT` · 커피 `COF` · 논커피 `NCOF` · 시그니처&스페셜 `SIG` · 에이드&스무디 `ADSM` · 티 `TEA` · 보틀 `BOT` · 세트 `SET` · 반제품 `PREP` |
| 새 분류 | 목록에 없는 분류를 쳐 넣으면 `ETC-001` 부터 붙는다 |
| 번호 | 기존 최대 + 1. **빈 번호는 재사용하지 않는다** — 지웠다 다시 만든 메뉴가 옛 SKU 를 물려받으면 채널 대조 기록과 엉킨다 |

같은 이름이 이미 있으면 막는다(SKU 만 다른 중복 메뉴 방지).

**메뉴 삭제**는 상세 창의 🗑 메뉴 삭제. 레시피·세트 구성·채널 예외까지 함께
지운다 — 남겨두면 어디에도 안 보이는 메뉴의 레시피가 유령처럼 남는다.

신메뉴를 만든 뒤 할 일: ① 레시피(자재/레시피 화면) → 원가 자동계산,
② 📝 플랫폼 소개글 한/영, ③ 채널에 등록(📋 작업지시서가 "채널에 없음"으로 잡아준다).

## 배달비를 어떻게 계산에 넣는가 (2026-07 수정 — 중요)

배달비 3,300원(배민·쿠팡 동일)은 **주문당 1회** 나가지, 메뉴마다 나가지 않는다.
그런데 처음 만든 화면은 메뉴마다 3,300원을 빼고 있었다. 그래서 베이글 단품이
"배민 −902원 적자"로 보였는데, **최소주문금액이 12,500원이라 베이글 하나만
담긴 주문은 애초에 존재할 수 없다.** 있지도 않은 상황을 계산한 셈이라 고쳤다.

### 지금 쓰는 3단 구조

| 지표 | 계산 | 무엇을 판단하나 |
|---|---|---|
| **원가율** | 재료원가 ÷ 판매가 | 레시피·매입가 문제. 배달비·수수료를 섞으면 메뉴 자체의 잘잘못을 못 가린다 |
| **기여마진** | 판매가 − 수수료 − 재료원가 | 이 메뉴가 주문 하나에 **얼마를 보태는가**. 배달비는 빼지 않음 |
| **주문 이익** | Σ기여마진 − 배달비 3,300 | 실제로 남는 돈. 🧮 버튼에서 확인 |

수수료(중개+결제+부가세)는 주문금액에 **비례**하므로 메뉴별로 나눠도 정확하다.
배달비만 정액이라 나눌 수 없다 — 그래서 주문 단위로 뺀다.

### 배달 손익분기 객단가

메뉴 표의 맨 오른쪽 열. *그 메뉴 수준의 원가율로 주문이 채워졌을 때* 배달비를
감당하는 최소 주문금액이다.

> 손익분기 객단가 = 배달비 ÷ (1 − 수수료율 − 원가율)

원가율 35% 기준으로 배민 약 6,100원 / 쿠팡 약 6,500원. **최소주문 12,500원의
절반 수준**이므로, 최소주문을 채운 주문은 사실상 전부 흑자다. 이 값이
12,500원을 넘으면 빨간색 — 그 메뉴 위주로 담긴 주문은 적자가 난다는 뜻.

현재 넘는 메뉴는 5개뿐 (원가율 60%대):
얼먹 코르네 3종(3,500원 / 원가 2,200원) · 후르츠 비타 · 풀파워 비타
(5,000원 / 원가 3,172원). → 가격 인상이나 레시피 조정 대상.

### 배분 모드 (선택)

🧮 창에서 "판매가 비례로 나눠 빼기"를 켜면, 메뉴 표의 기여마진에서
`배달비 × (판매가 ÷ 평균객단가)` 만큼을 추가로 뺀다. **전부원가 관점**이라
가격을 올릴지 결정할 때 쓴다. 기본은 꺼짐(기여마진만 봄).

- **평균객단가**는 🧮 창의 "📥 실측 객단가" 버튼으로 `orders` 테이블의 실제
  주문금액 평균(최근 90일)을 불러온다. 없으면 최소주문금액으로 보수적 계산.
- 균등 배분(배달비 ÷ 품목수)은 **쓰지 않는다.** 2,500원 아메리카노와
  15,000원 세트에 같은 배달비를 물리면 저가 음료가 무조건 적자로 보이는데,
  음료는 단독으로 주문을 만들지 않으므로 그 판단은 틀렸다.

## 데이터 출처와 한계 (중요)

- 메뉴 정본: 구글드라이브 `베어글스_메뉴DB` 시트 (2026-07 기준 135개)
- 재료원가: `베어글스_메뉴원가계산기_코스트.xlsx` 레시피(2025-09 단가) 91개 매칭.
  **재료 매입가가 바뀌었으면 오래된 값이다** — 정산서 보며 갱신할 것
- **원가 미입력 44개**: 크림치즈(배치 원가만 있어 1인분 환산 필요)·베이글
  일부·세트·티 일부 → 화면에서 "입력" 눌러 채우기
- 배달가: 기존 쿠팡 시트의 가격(첫 25개, 순서 매칭이라 **확인 필요**),
  나머지는 비어 있음 → 채워야 배달 마진이 정확해짐
- 배달비는 배민·쿠팡 모두 **3,300원**(사장님 확인, 2026-07). 중개·결제 수수료
  (배민 6.8%+3%, 쿠팡 9.8%+3%, 네이버 2%+3.3%)는 **공개 요율 기준 추정**이라
  정산 내역과 대조해 ⚙에서 수정할 것
- 인건비·임대료·광고비·즉시할인 부담은 미포함. 특히 배민 즉시할인(5,000원 등)은
  **전액 가게 부담**이라 적용 메뉴는 별도로 따져야 함

### 매입 단가를 갱신할 때 (부가세 주의)

레시피에 박힌 단가는 2025-09 기준이라 오래됐다. 정산서·발주서를 보고
`/menu/ingredients` 화면에서 구매량·구매가를 고치면 그 자재를 쓰는 메뉴의
원가가 즉시 재계산된다.

발주서의 단가 컬럼이 보통 셋이라 헷갈리기 쉽다:

| 컬럼 | 뜻 |
|---|---|
| 공급가 | 부가세 전 매입가 |
| 부가세 | 공급가의 10% (면세 품목은 0) |
| 단가 합계 | 실제로 나가는 돈 = 공급가 + 부가세 |

**재료원가의 기준은 `공급가`(부가세 별도)다.** 매입 부가세는 매입세액공제로
돌려받으므로 실제로 부담하는 원가가 아니다. `단가 합계`를 쓰면 모든 원가가
10% 부풀려진다. (판매가도 부가세를 포함하니 양쪽 다 세전으로 봐야 비율이 맞다.)

규격이 `1.2kg*8ea/box` 처럼 적혀 있으면 **한 번 주문하면 손에 들어오는 양**을
구매량으로 적는다. 박스 포장 설명의 곱셈수까지 곱하면 원가가 몇 배로 틀어진다.

### 플랫폼 소개글 (한/영)

네이버·배민·쿠팡·토스 키오스크에 메뉴 설명을 넣을 때마다 직원이 새로 쓰다 보니
채널마다 문구가 달랐다. 메뉴를 탭하면 나오는 **📝 플랫폼 소개글**에 한 번
적어두고 📋 로 복사해 쓴다(2026-08-17, `menu_items.intro_ko / intro_en`).

- 기존 `description` 은 정본용 긴 글(3~4줄 + 추천 크림치즈)이라 그대로 쓰기엔 길다.
  소개글은 **한 문장**이 기준.
- 영문은 토스포스의 `키오스크 상품설명 🇺🇸` 칸에 그대로 들어간다.
- 판매중 176개는 채워 두었다. 문체는 **정보 중심으로 단정하게**(사장님 선택
  2026-08-17) — 재료·특징을 분명히, 과장·이모지 없이 한두 문장.

**문장은 AI(무료 제미나이)가 쓴다.** 규칙으로 찍어봤더니 "~살아 있는
~입니다"만 반복돼 카페 문구가 아니었다. 새 메뉴를 만들면 자동으로 초안이
들어가고, 기존 메뉴는 상세 창의 **✨ 초안 만들기**로 채운다. 전체를 다시
쓰려면:

    python scripts/rewrite_intros.py              # 판매중 전부
    python scripts/rewrite_intros.py --only-empty # 비어 있는 것만

⚠️ **제미나이 무료 한도는 모델마다 다르다.** 기본 `gemini-flash-latest` 는
`gemini-3.7-flash` 로 풀려 **하루 20건**뿐이라 메뉴 176개를 못 돌린다.
소개글은 `flash-lite` 로 고정해 두었다(`.env` 의 `INTRO_GEMINI_MODEL` 로 변경).
AI 를 못 쓰면 규칙 초안(`service/menu_intro.py`)으로 떨어진다 — 버튼이
먹통이 되는 것보다 낫다.

⚠️ **"갓 구운 · 수제 베이글 · 매일 직접 반죽"은 쓰지 않는다.** 베이글은 본사 냉동
납품이고 매장에서는 그릴 토스팅만 한다([knowledge/브랜드.md](knowledge/브랜드.md)).
"주문 즉시 토스팅"은 사실이라 쓸 수 있고, **수제청·크림치즈는 실제로 만드니**
'수제'를 써도 된다.

### 반제품 — 매장에서 만들어 소분해 파는 것

크림치즈처럼 **한 번 만들어 여러 개로 소분**하는 것은 반제품으로 둔다.
`/menu/ingredients` 의 **반제품 만들기**로 만들면 자재 1줄 + 제조용 메뉴
1줄이 이름으로 짝지어진다. 제조 레시피를 채우면 배치 원가가 자동으로 잡히고,
판매 메뉴에서는 그 반제품을 재료처럼 쓴다.

**산출량은 개수(ea)가 아니라 g 으로 잡는다.** 개수로 잡으면 소분 1개짜리만
표현되고, 같은 반제품을 다른 크기로 파는 순간 막힌다 — 실제로 크림치즈에
L(200g) 이 10종 있는데 "200g은 몇 개인가"를 쓸 수 없었다(2026-08-16).

플레인 크림치즈 실례:

| | |
|---|---|
| 배치 | 프레리팜 크림치즈 600g + 휘핑크림 90g + 레몬주스 10g + 설탕 50g |
| 배치 원가 | 6,744원 · 산출 **750g** (= 소분 12개) |
| g당 | 8.99원 |
| 기본(62.5g) | 562원 → 매장가 2,300원 · 원가율 **24.4%** |
| L(200g) | 1,798원 → 매장가 9,200원 · 원가율 **19.5%** |
| 샌드위치(30g) | 270원 |

크림치즈 매입가가 오르면 배치 원가가 다시 잡히고, **이 반제품을 쓰는 메뉴가
전부 같이 갱신된다.**

주의: 같은 재료를 반제품과 일반 자재로 **두 번 두지 않는다.** 전에 '플레인
크림치즈(60g)' 자재가 따로 남아 샌드위치 2개가 그쪽을 쓰고 있었다. 매입가가
바뀌면 한쪽만 갱신돼 원가가 조용히 어긋난다 — 반제품으로 합쳤다.

### 짝 맞추기 — 왜 자동으로 안 잇는가

발주 품목명과 우리가 쓰던 자재명이 다르다(`건크랜베리` ↔ `건조 크랜베리`).
이름이 닮았다고 자동으로 이으면 **틀린 자재에 붙어 원가가 조용히 망가진다** —
실제로 `스위트콘`(2.82원/g)과 `스위트콘스프`(19원/g)는 이름이 비슷하지만
6배 차이 나는 다른 물건이다. 그래서 사람이 눈으로 확인해 고르게 두었다.

`/menu/ingredients` → **짝 맞추기** 탭. 대기열은 **레시피에 실제로 쓰이는
자재를 먼저** 보여준다 — 안 쓰는 자재를 이어봐야 메뉴 원가는 안 바뀐다.
앞쪽 몇 건만 끝내도 원가는 거의 맞아간다.

## 지금 보이는 문제 (개선 후보)

배달비 계산을 바로잡은 뒤 기준으로 다시 정리한 것이다.

1. **원가율 60%대 5개** — 얼먹 코르네 3종·후르츠 비타·풀파워 비타.
   유일하게 손익분기 객단가가 최소주문금액을 넘는다. 가격 인상 또는 레시피 조정.
2. **샌드위치 원가율 40%+ 다수** (풀드포크 40.3%, 잠봉뵈르 치아바타 44.9%)
   — 목표 38% 초과. 흑자이긴 하나 목표 대비 초과. 가격·레시피·매입가 점검.
3. **목표 원가율 초과 52개 / 원가 미입력 44개** — 미입력을 채우면 숫자가 바뀐다.
   먼저 채우고 다시 볼 것.
4. ~~베이글·크림치즈 단품 역마진~~ — **오판이었다.** 배달비를 메뉴마다 빼서
   생긴 착시. 실제로는 전부 흑자(플레인 베이글 배민 기여마진 +2,399원).

## 채널 노출 메뉴 수집 · 대조 (📡 버튼)

- `/menu` 화면의 **📡 채널 대조** → "채널수집 요청"을 누르면 집 PC 일꾼이
  배민·쿠팡 사장님 포털(로그인 세션 재사용) + 네이버플레이스(공개 페이지)에서
  **실제 노출 중인 메뉴·가격**을 긁어 온다 (`jobs.kind = menu_collect`).
- 수집이 끝나면 같은 창에서 정본과의 차이를 보여준다:
  이름 다름 · 가격 다름 · 정본에 없음 · 채널에 없음.
- 사전 준비: ① `supabase/migrations/006_menu_snapshots.sql` 실행,
  ② 집 PC `.env` 에 `NAVER_PLACE_ID=2023997350` 추가
  (베어글스 송도 타임스페이스점의 네이버 플레이스 ID. 없으면 네이버만 건너뜀),
  ③ 집 PC에서 최신 코드 pull 후 일꾼 재시작.
- 첫 수집은 실패할 수 있다(플랫폼 메뉴 API 구조를 실물로 확인한 적 없음).
  실패하면 `debug/menu_*.txt` 덤프가 남고 새벽 자동 점검이 고치도록 돼 있다.

## 채널 통일 작업 순서 (권장)

1. `/menu`에서 정본 메뉴명·가격 확정 (원가 미입력부터 채우기)
2. 배민 사장님포털 → 메뉴 관리에서 정본과 다른 이름/가격 수정
3. 쿠팡이츠 스토어포털 → 동일
4. 네이버 스마트플레이스 → 동일
5. 채널이 정본과 일부러 다르게 가야 하면(플랫폼 정책 등) 화면의
   "채널 예외"에 기록해 둔다 — 기록 없는 차이는 실수로 간주
