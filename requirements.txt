# 베어글스 카페 AI 비서 - 의존성 목록

# 크롤링 (Playwright — 로그인된 Chrome 에 CDP attach)
# attach 모드는 실행 중인 Chrome 에 붙으므로 별도 브라우저 다운로드 불필요.
# profile 모드는 channel="chrome"(시스템 Chrome) 사용.
playwright>=1.49
beautifulsoup4==4.12.3
requests==2.32.3

# 데이터베이스 (Supabase)
# 2.31+ : 신형 sb_publishable_/sb_secret_ 키 형식 지원
supabase>=2.31.0
websockets>=13

# 텔레그램 봇
python-telegram-bot==21.4

# 스케줄러
APScheduler==3.10.4

# AI 비서 (LLM) - Anthropic Claude
anthropic>=0.40.0

# 웹 대시보드 (로컬 운영 UI)
flask>=3.0

# 환경 변수
python-dotenv==1.0.1

# 테스트 (개발용) — 순수 로직/안전계약 회귀 테스트. `python -m pytest tests/`
pytest>=8.0
