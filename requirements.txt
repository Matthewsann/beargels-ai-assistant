# 베어글스 AI 비서 - 통합 의존성 (매출·리뷰 봇 + 인스타 파이프라인)
# 블로그 자동화(automation/)는 automation/requirements.txt 를 따로 사용합니다.

# --- 크롤링 (Playwright — 로그인된 Chrome 에 CDP attach) ---
playwright>=1.49
beautifulsoup4==4.12.3
requests==2.32.3

# --- 데이터베이스 (Supabase) ---
supabase>=2.31.0
websockets>=13

# --- 텔레그램 봇 ---
python-telegram-bot>=21.4

# --- 스케줄러 ---
APScheduler>=3.10.4

# --- AI (Anthropic Claude) ---
anthropic>=0.49.0

# --- 환경 변수 ---
python-dotenv>=1.0.1

# --- 인스타 파이프라인: 구글 드라이브 OAuth + 미디어 처리 ---
google-api-python-client>=2.100.0
google-auth>=2.23.0
google-auth-oauthlib>=1.2.0
httpx>=0.27.0
Pillow>=10.0.0
imageio-ffmpeg>=0.5.1

# --- 인스타 릴스 웹 파이프라인 (run_web.py) ---
fastapi>=0.110
uvicorn>=0.29
python-multipart>=0.0.9

# --- 테스트 (개발용) ---
pytest>=8.0
