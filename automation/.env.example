# 이 파일을 복사해서 .env 로 저장하고 값을 채우세요.  (cp .env.example .env)
# .env 는 절대 깃에 올라가지 않습니다(.gitignore 처리됨).

# Claude(Anthropic) API 키 — 글 자동 생성에 사용
# https://console.anthropic.com 에서 발급
ANTHROPIC_API_KEY=sk-ant-여기에_키_입력
