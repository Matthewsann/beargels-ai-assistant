-- ---------------------------------------------------------------------------
-- 실제 등록된 답글을 학습 데이터로 (2026-08-10, 사장님 요청)
--
-- 왜: 배민·쿠팡에 이미 달려 있는 사장님 답글은 '검수까지 끝난 최종본'이다.
--     크롤러가 본문(platform_reply)을 함께 수집해 두면, 새벽 답글 공부가
--     이걸 모범 예시·스타일 학습에 쓸 수 있다.
--     (답글 여부 boolean 은 기존 platform_replied 가 담당 — 본문만 추가)
-- ---------------------------------------------------------------------------

alter table public.reviews add column if not exists platform_reply text;
