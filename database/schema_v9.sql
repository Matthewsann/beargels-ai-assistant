-- ---------------------------------------------------------------------------
-- 회의 할 일에 '기타 메모' 추가 (2026-08-30)
--
-- Supabase SQL Editor 에 붙여넣고 Run. 여러 번 실행해도 안전합니다.
--
-- 왜: 사장님이 회의 페이지 목표를 다시 확인해 주셨다 — 업무 등록 항목은
--     담당자 / 마감일 / 업무내용 / **기타메모** 네 가지. 앞의 셋은 이미
--     meeting_tasks(owner/due_date/content)에 있었고 메모 칸만 없었다.
-- ---------------------------------------------------------------------------

alter table public.meeting_tasks
    add column if not exists memo text;
