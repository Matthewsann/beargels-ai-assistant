# 직원용 리뷰 답글 웹서비스(클라우드) 의존성 — 최소한만.
# 답글 생성은 집 PC 일꾼이 하므로 anthropic·playwright 는 필요 없다.
Flask>=3.0
python-dotenv>=1.0
supabase>=2.0
gunicorn>=21.0
Pillow>=10.0.0   # 메뉴 사진 채널 규격 변환 — 지금은 PA에 우연히 깔려 있어 돌지만 명시해 둔다
