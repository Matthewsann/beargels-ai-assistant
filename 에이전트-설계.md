# 🤖 베어글스 블로그 파이프라인 — 에이전트 분류 및 설계

> 기준 구조 (사장님 정의):
> **시장조사(트렌드서치) → 기획(주제뽑기) → 선택(사람) → 본문작성 → 이미지 생성·보정 → 임시저장 → 검수·발행(사람) → 피드백(노출/유입분석)**
>
> 이 구조는 그대로 채택하고, 아래 **4가지를 보완**했습니다.

---

## 0. 구조 검토 — 좋은 점과 보완점

**✅ 좋은 점**
- 사람 개입이 **2곳(선택·발행)** 으로 최소화 — 반자동의 정석
- 선택을 **본문작성 앞**에 둠 → 안 쓸 글을 생성하지 않아 API 비용·낭비 없음
- 피드백이 맨 뒤에 있어 학습 루프의 자리가 잡혀 있음

**🔧 보완 4가지 (반영됨)**
| # | 보완 | 이유 |
|---|------|------|
| 1 | **AI 검수를 '임시저장 앞'에 추가** | 사람 검수(⑦)가 가벼워짐. 미달 글은 AI가 먼저 고쳐서 옴 → 사장님은 사진+클릭만 |
| 2 | **실물사진 입력 관문 명시** | "이미지 생성·보정" 단계에 폰 사진이 들어오는 입구(media/inbox)가 있어야 함. 음식은 실물사진이 원칙(D.I.A.) |
| 3 | **피드백 → 시장조사·기획으로 루프 연결** | 분석이 리포트로 끝나면 무의미. 유입 키워드가 다음 주제 후보에 자동 주입되어야 "스스로 좋아지는" 파이프라인 |
| 4 | **기획 단계에 중복회피·키워드전략 내장** | 같은 주제 반복은 유사문서 위험. 주제마다 대표1+세부2~3 키워드를 기획 시점에 확정 |

---

## 1. 최종 아키텍처

```
[지식·자산 레이어] ─ 네이버-SEO-지식.md · 브랜드-가이드.md(세계관) · brand/(팔레트·베어그리)
        │ (모든 자동 단계에 주입)
        ▼
⓪ 시장조사 [자동]     트렌드·연관검색어 수집 → data/trends.yaml
        ▼
① 기획   [자동]      주제안 N개 생성: 제목가안+키워드+선정근거 (중복회피·유형배분)
        ▼
② 선택   [사람·5분]   주제안 목록에서 번호로 선택 → content-plan.yaml 확정
        ▼
③ 본문작성 [자동]     SEO+세계관 주입 본문 생성 (1,500자+, 사진위치 표시)
        ▼
④ AI검수 [자동]★보완  체크리스트 심사·점수화, 미달 시 자체 수정 → 창고(ready)
        ▼
⑤ 이미지 [자동+사람]   썸네일·포스터 생성(공식 팔레트) ┃ 폰 실물사진 → inbox → 자동보정·영상
        ▼
⑥ 임시저장 [자동]     네이버 에디터 입력 + 임시저장 (또는 예약발행 세팅)
        ▼
⑦ 검수·발행 [사람·10분] 임시저장 글에 사진 삽입 → 최종 확인 → 발행/예약 클릭
        ▼
⑧ 피드백 [자동]       조회수·유입키워드 분석 → 리포트
        │
        └──────▶ ⓪시장조사 시드·①기획 소재 풀에 자동 반영 ★보완 (학습 루프)
```

---

## 2. 에이전트 분류표

| 단계 | 에이전트 | 구현 파일 | 상태 |
|------|---------|----------|------|
| ⓪ 시장조사 | **트렌드서처** — 시드 키워드로 네이버 자동완성·연관어 수집 | `trend_search.py` | ✅ 신규 |
| ① 기획 | **플래너** — 트렌드+캘린더+기존글로 주제안 생성 | `propose_topics.py` | ✅ 신규 |
| ② 선택 | **사람** — 번호 선택 → content-plan.yaml 확정 | `propose_topics.py` (대화형) | ✅ 신규 |
| ③ 본문작성 | **작가** — SEO·세계관 주입 생성 | `generate_post.py` | ✅ |
| ④ AI검수 | **에디터/QC** — 점수화·자체수정 | `review_post.py` | ✅ 신규 |
| ⑤ 이미지 | **미디어** — 보정·영상·썸네일·캐릭터 | `enhance_photos.py` 외 3종 | ✅ |
| ⑥ 임시저장 | **퍼블리셔** — 에디터 입력+임시저장/예약 | `naver_autodraft.py`, `schedule_publish.py` | ✅ |
| ⑦ 검수·발행 | **사람** — 사진 삽입+발행 클릭 | (네이버 화면) | — |
| ⑧ 피드백 | **애널리스트** — 노출/유입 분석→리포트→학습 키워드를 ⓪①에 자동 주입 | `feedback.py` | ✅ v1 (통계는 화면 보고 입력, 자동 수집은 로드맵) |

**통신 방식**: 에이전트 간 직접 호출 없이 파일로 전달
(`data/trends.yaml` → `content-plan.yaml` → `library/NNNN/{post.json, meta.yaml}` → 네이버)
**상태 머신**: `ready → scheduled → published` (meta.yaml, 검수점수 `review_score` 포함)

---

## 3. 실행 시나리오 (사장님 기준)

### 매일 자동 (작업 스케줄러, 손 안 댐)
- `stockpile.bat` : 상시소재 생성+검수 → 창고 적재 (선택과 무관하게 재고 유지)
- `trend_search.py` : 트렌드 수집 (기획 재료 갱신)

### 주 1회 · 세션 A (기획 확정, ~5분)
```
py src\propose_topics.py
 → 주제안 10개(제목가안·키워드·근거) 표시 → "1,4,7" 선택
 → content-plan.yaml 자동 작성 → 이어서 생성+검수까지 자동 실행
```

### 주 1회 · 세션 B (발행, ~10분)
```
weekly_publish.bat  (예약발행)  또는  py src\naver_autodraft.py --all  (임시저장만)
 → 네이버에서 사진 삽입 → 발행/예약 클릭
```

### 수시 (사진 찍은 날)
```
폰 사진 → media\inbox\ → media.bat  (보정+영상 자동)
```

---

## 4. ⑧ 피드백 에이전트 — 구현 현황 (v1 완료)

**지금 동작하는 것 (`feedback.py`)**
1. `--open`: 자동화 크롬으로 크리에이터 어드바이저를 열고 입력 양식(`data/stats-input.yaml`) 생성
2. 사장님이 화면의 조회수·유입검색어를 양식에 붙여넣기 (월 1회, 5분)
3. 실행 시: `data/리포트_YYYY-MM.md`(성과 랭킹·유입 키워드 TOP) +
   `data/keywords-learned.yaml`(성과 검증 키워드) 생성
4. **학습 루프 자동 연결됨**: 학습 키워드가 ⓪trend_search 시드에 합류하고,
   ①propose_topics 의 기획 근거("실제 유입 검색어, 우선 반영")로 주입됨

**로드맵 (v2)**: 통계 화면 자동 스크래핑 — ⑥임시저장 자동화가 PC에서 안정화된 뒤
같은 브라우저 세션으로 셀렉터를 맞춰 `--open`+수동입력을 완전 자동 수집으로 교체.

**사진 입구 (보완2 확정)**: 구글드라이브 `베어글스_블로그_사진함` 폴더 지정 방식.
폰에서 드라이브 업로드 → PC '데스크톱용 Google Drive' 동기화 → `config.yaml` 의
`media.inbox` 경로를 그 폴더로 지정 → 파이프라인이 자동으로 가져감(중복 보정 방지 내장).

---

## 5. 로드맵 요약

| 순서 | 작업 | 상태 |
|------|------|------|
| 1 | ⓪~⑦ 전 단계 구동 (이번 커밋) | ✅ |
| 2 | PC에서 ⑥ 셀렉터 튜닝·안정화 | 진행 예정 |
| 3 | ⑧ 피드백 에이전트 (통계 수집→리포트→루프) | 🔜 |
| 4 | 인스타 동시 발행, 댓글 답변 초안 | 💡 |
```
