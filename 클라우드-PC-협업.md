# 클라우드에서 개발하고, 집 PC로 실행하기

크롤링은 **로그인된 크롬**이 있어야 해서 집 PC에서만 돌아갑니다.
하지만 코드 개발은 클라우드에서 계속할 수 있습니다. 이렇게 나눠서 씁니다.

```
집 PC                     GitHub                클라우드 세션
크롤링 실행  ──→  리포트 push  ──→  리포트 읽고 원인 분석
   ▲                                              │
   └────────  고친 코드 pull  ←──  코드 수정 push ─┘
```

집 PC에는 **Claude가 필요 없습니다.** 파이썬만 돌면 됩니다.

## 집 PC에서 하는 일

`worker\menu_report.bat` 하나만 실행하면 됩니다. 이 파일이 알아서:

1. `git pull` — 클라우드에서 고친 코드 받기
2. 배민·쿠팡·네이버 메뉴 수집
3. 결과 요약을 `reports\latest-menu.md` 에 작성
4. 그 리포트를 GitHub에 push

실행 전에 **`scripts\launch_chrome.bat` 으로 디버그 크롬을 띄우고
배민·쿠팡·네이버에 로그인**되어 있어야 합니다. 안 되어 있으면 리포트에
"디버그 크롬 꺼져 있음" 또는 "로그인 만료 의심"이라고 찍힙니다.

### 매일 자동으로 돌리려면

윈도우 **작업 스케줄러**에 `worker\menu_report.bat` 을 등록하세요
(예: 매일 04:00). 그러면 사람 손 없이 매일 리포트가 GitHub에 올라옵니다.

## 클라우드 세션에서 하는 일

```bash
git pull
```

받은 뒤 `reports/latest-menu.md` 를 읽으면 PC에서 무슨 일이 있었는지 그대로
보입니다. 채널별 건수, 실패 원인, 사람이 할 일이 적혀 있습니다.
문제가 보이면 코드를 고쳐서 push하고, PC는 다음 실행 때 자동으로 받아갑니다.

## 리포트에 무엇이 담기나

- 채널별 수집 건수와 상태 (✅ 정상 / ⚠️ 0건 / ❌ 실패)
- 실패 원인 분류 — 크롬 미실행, 로그인 만료, 화면 구조 변경
- 사람이 해야 할 조치
- 오류 상세 (있을 때만)

**메뉴 이름·가격 같은 실제 데이터와 쿠키·토큰은 넣지 않습니다.** 요약만 올립니다.
`logs/` 와 `debug/` 는 지금처럼 GitHub에 올라가지 않습니다.

## 사람이 직접 해야만 하는 것

**로그인이 풀렸을 때**는 PC가 혼자 못 고칩니다.
`scripts\launch_chrome.bat` 으로 크롬을 띄우고 해당 사이트에 다시 로그인하세요.
리포트에 어느 채널인지 찍혀 있습니다.

## 관련 파일

| 파일 | 역할 |
| --- | --- |
| `worker\menu_report.bat` | PC에서 실행 — 수집 → 리포트 → push |
| `scripts\menu_report.py` | 수집하고 리포트를 만드는 본체 |
| `reports\latest-menu.md` | 가장 최근 실행 결과 (항상 이 파일만 보면 됨) |
| `reports\menu-YYYYMMDD-HHMM.md` | 실행별 기록 (이력 추적용) |
