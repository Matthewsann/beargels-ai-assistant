# 작업 규칙 (모든 Claude Code 세션 공통)

이 프로젝트는 여러 곳(클라우드 채팅 여러 개 + 집 PC)에서 번갈아 개발합니다.
충돌과 작업 누락을 막기 위해 **모든 세션은 아래 규칙을 지킵니다.**

## 브랜치
- 작업 브랜치는 **`main` 하나**다. 세션을 시작하면 브랜치가 맞는지 확인한다.
- 인스타 릴스 파이프라인도 여기 들어와 있다(`sns_automation/`, `run_web.py`,
  `web/`). 예전엔 별도 폴더 `beargels-sns`(브랜치
  `claude/bearglus-instagram-automation-qabeuy`)에서 따로 개발했는데,
  **2026-08-28 에 이 리포로 흡수했다** — 같은 가게의 같은 비서라 .env·지식금고·
  AI 계층(`llm.py`)을 따로 둘 이유가 없었다. 그 폴더와 브랜치는 이제 없다.

## 동기화 리듬 (가장 중요)
1. **작업 시작 전:** 반드시 `git pull` 로 최신 코드를 먼저 받는다.
2. **작업 끝날 때:** 변경사항을 `git commit` 후 `git push` 로 올린다.
3. 한 번에 **한 곳에서만** 작업한다. 다른 곳에서 이어가려면 먼저 push 하고 넘어간다.

사용자가 "이어서 하자"고 하면, 코드를 건드리기 전에 항상 `git pull`부터 실행한다.
작업을 마치거나 사용자가 자리를 뜰 낌새면, 묻지 말고 commit+push로 저장해 둔다.

## 비밀 파일 (동기화 안 됨 / 각 기기 로컬 전용)
`.env`, `client_secret.json`, `token.json` 은 `.gitignore` 로 제외되어 GitHub에
올라가지 않는다. 즉 기기마다 따로 둔다. 없다고 해서 pull로 받아지지 않는다.

## 리뷰 답글 페이지의 목표 (사장님 확정 2026-08-28)
`/todo` `/history` `/review` (배민·쿠팡 리뷰 답글 화면군)를 손댈 때는
항상 이 기준으로 판단한다 — 새 버튼·새 규칙이 이 세 가지 중 무엇을
얼마나 좋아지게 하는지 먼저 따져본다.

1. **빠르게** — 관리자가 플랫폼에서 리뷰 하나하나 직접 답글을 쓰지 않고,
   한 곳에서 AI 도움을 받아 빠르게 등록한다.
   목표치: 리뷰 10건 답글 ≈10분 → 이 페이지로 **≈1분**.
   (⚠️ 아직 실측 안 됨 — 다음에 손댈 때 일괄 등록 10건 소요 시간을 재서
   병목을 찾을 것. 배민 등록 1건에 10초 안팎 걸리는 걸 로그로 본 적 있음.)
2. **안전하게** — 문제 될 만한 리뷰(이물질·환불·법적 등 에스컬레이션)는
   꼼꼼히 걸러 자동 게시하지 않고, 관리자가 직접 확인 후 게시되게 한다.
3. **놓치지 않게** — 리뷰 속 고객의 요청사항·반응은 답글만 달고 끝내지
   말고, 관리자가 확인해 전 직원에게 쉽게 전파할 수 있게 한다
   (`assistant/customer_requests.py` + 리뷰 현황의 '고객 요청사항' 칸).

## 네이버 블로그 페이지의 목표 (사장님 확정 2026-08-29)
`/blog` (네이버 블로그 자동화 화면군)를 손댈 때는 항상 이 기준으로 판단한다.

> **"사장님이 버튼만 눌러도 상위노출되는 글이 사진·영상까지 갖춰 자동으로
> 임시저장되고, 실제 발행 예약을 거는 것은 사람이 최종적으로 한다.
> 그리고 발행할수록 시스템이 스스로 똑똑해진다."**

화면 흐름(사장님 확정 2026-09-17, 초보자 기준 UX): **홈** 1단계 키워드 정하기(내 키워드 +
💡 추천 담기) → 2단계 [🎯 주제 뽑기] → 3단계 주제 카드에서 [✍️ 이 주제로 초안 만들기] →
📝 진행 중인 글(카드마다 다음 할 일) → ✅ 발행 완료. **글 화면** ③ 사진·영상 자리마다 폰 업로드
→ ④ 품질 채점 → ⑤ 시각 확인 후 [📅 예약 발행]. 홈·글 화면 맨 위 **'👉 지금 할 일' 배너 하나**가
다음에 누를 버튼을 말한다(`app._blog_next`, `_blog_step` 은 사진 자리까지 본다).
**📸 촬영 목록 `/blog/shots`**(사장님 2026-09-17: "매장에서는 초안을 보면서 하나씩 찍을 수 없다"): 발행 전
모든 초안의 사진 자리를 한 화면에(글별/장소별), 자리마다 [📷 사진 올리기] 하나 — 올리면 그 글의 그 자리에
들어가고 목록에서 사라진다. 글 화면과 같은 업로드 길(`blog_post_photo` upload_after + `next=shots`)이고,
낡은 화면이면 `_wish_slot` 이 같은 설명의 자리를 다시 찾는다(없으면 올리기 전에 멈춘다). 결과는 하단 고정
알림, 다 채운 글은 '🎉 → 품질 채점' 안내. [🖨 A4 체크리스트로 인쇄](`?print=1` → `blog_shots_print.html`, 지금 보기 그대로·흑백·☐ 체크칸·줄은 쪽 사이에서 안 잘림). 입구는 홈 '지금 할 일' 배너·진행 중인 글 위·글 화면 목적 카드.
**✅ 올린 사진**(사장님 2026-09-17 "잘못 선택했는데 바꿀 수가 없네"): 촬영 목록의 글마다 접힌 칸에서 올린 사진을
미리보기로 확인하고 [🔁 다른 사진으로 바꾸기]·[↩ 빼기]. 부탁 자리에 넣을 때 원래 부탁(설명+팁)을
`menu_settings.blog_shot_origin` 에 적어 두어(`app._shot_origin_save`), 빼면 그 '찍을 사진' 자리가 되살아난다
(기록 없는 옛 사진은 그냥 빠진다). 인쇄본엔 찍을 것 있는 글만.
컷 메모 뒤의 `(팁: 찍는 요령)` 은 메모의 일부다 — 화면엔 💡 로 보이고 발행·채점에선 같이 걷힌다
(`blog_media.WISH_RE`, `app._WISH_BLOCK`). 사진 부탁 줄은 `_blog_chunks` 가 항상 제 토막으로 뗀다 —
개수(홈·카드·목록)는 전부 이 기준. 업로드 JS 는 `_blog_upload_js.html` 하나(한 번에 하나만·라벨 글자만
바꿈·리다이렉트일 때만 이동). ⚠ 새 템플릿은 `deploy.yml` 업로드 목록에 넣어야 PA 에 올라간다. 새 버튼·칸을
넣을 때 이 순서를 흐트러뜨리지 말고, 고급 기능은 '더 보기'에 접는다. 용어: 키워드·주제·초안·
사진·영상·품질·예약 발행(글감·임시저장 같은 말은 화면에 안 쓴다).
**속도**(검진 2026-09-18, 실측 홈 7~11초·글 3~5초): PA→Supabase 왕복이 ~0.4초라 **조회 횟수가 곧 시간**이다.
같은 요청 안 같은 설정·잡 조회는 한 번만(`app._once`·`db.get_setting` 요청 메모, 쓰기가 메모를 지움), 화면은
`_prefetch(settings=…, **조회)` 로 한꺼번에(홈 17회 차례→1파도, 글 9→1, 촬영 목록은 발행·예약 글을 서버에서 거름).
발행 완료 목록은 본문 없이(`_PUBLISHED_COLS`), 순위는 2분 캐시. 새 화면·새 조회를 넣을 땐 `prof_blog.py` 식으로
httpx 호출 수를 먼저 세고, 차례로 부르지 말고 `_prefetch` 에 넣는다.

네 축으로 쪼개 판단한다:
1. **상위노출** — 네이버 SEO(C-Rank·D.I.A.·스마트블록) 기준으로 실제 경쟁력
   있는 글인가. 분량·키워드·제목·구조·태그가 기준을 넘는가.
   **키워드는 사장님이 먼저 정한다**(사장님 2026-09-16): 블로그 홈 맨 위 '🔑 타겟 키워드'
   (`menu_settings.blog_target_keywords`)에 직접 넣고, 체크한 키워드로 [글감 추천]
   (잡 `blog_recommend` payload `keywords` → 그 키워드를 대표 키워드로 하는 글감을 앞 번호에) ·
   [경쟁·검색량 실측](새 잡 `blog_research` → 그 키워드만 네이버 실측, 주간 조사 파일에 합침) ·
   [바로 초안](첫 체크 = 대표, 나머지 = 세부)을 한다. 시스템 추천은 **따로** '💡 추천 키워드'
   칸에 — 네이버 실측 요약(`menu_settings.blog_keyword_research`, 집 PC 가 `naver_search.
   publish_summary` 로 올림. ⚠ `research()` 안에서 부르지 않는다 — 테스트가 실제 설정을 0건으로
   덮었다) 기회 순 + 유입 검색어. [＋]로 타겟에 담는다. 추천 프롬프트에도 이제 실측·유입이 들어간다.
2. **사진·영상까지 — 단, 사진은 사장님이 고른다** (사장님 2026-09-15, 8/29 의
   '자동으로 들어가게'를 번복: "사진함에서 사진 너가 넣지 마, 내가 넣을게").
   AI 의 몫은 **어디에 어떤 사진이 들어가면 좋은지** 자리마다 코멘트를 남기는 것까지
   (`[📸 부탁: 구체적 컷 설명]` 7~9곳, 절마다 최소 1곳). 글 화면 맨 위 '🎯 이 글의 목적'
   카드가 주제·강조 키워드(본문 측정값)·찍어 올릴 사진 목록을 보여 주고, 부탁 카드엔
   코멘트와 [📷 사진·영상 올리기] 버튼 하나만 있다 — 폰에서 고르면 바로 올라가 그 자리에
   들어간다(사진 1600px JPEG + 640px 미리보기, 영상 mp4/mov 50MB 까지 그대로).
   ⚠️ **사진함 고르기는 없앴다**(사장님 2026-09-16 "그냥 바로 업로드하는 게 편해") —
   글 화면 고르기 패널·홈의 [사진함 다시 보기]·`blog_media` 잡·추천 프롬프트의 사진 목록 전부.
   드라이브 소재함 폴더는 업로드 파일이 내려오는 저장소(`업로드/글N/`)로만 남는다
   (`blog_media.pull_uploads`). 자동 채움(옛 `fill_photos`)도 걷어냈다 — 되살리지 말 것.
   채점기는 부탁 자리도 '사진 위치'로 세고, 네이버 발행본엔 부탁 메모가 안 나간다.
   **올리는 사진·영상에 정보를 붙인다**(사장님 2026-09-17): `blog_jobs.build_blocks` 가 파일 이름을
   「대표키워드-NN.jpg」로, 사진 설명·영상 내용은 바로 앞 문장으로, 영상 제목은 「키워드 | 앞 문장」,
   영상 태그는 대표·세부 키워드로 만든다. 에디터(`naver_autodraft`)는 **2026-09-17 프로필 사본으로
   실측한 선택자**(SE ONE 1.78.1)로 넣는다: 사진 설명 `.se-component.se-image .se-caption
   .se-text-paragraph`(삽입 직후 숨겨져 있어 img 를 먼저 클릭), 영상 제목 `input#nvu_inp_box_title`·
   정보 `textarea#nvu_inp_box_description`·태그 `button#nvu_inp_box_tag` → `input.nvu_tag_inp` + Enter.
   ⚠ 영상 '완료' 버튼은 업로드 시작 즉시 보인다 — `.nvu_file` 이 wait/upload/extract 를 벗어나거나 버튼이
   `nvu_btn_type2` 가 될 때까지 기다린다. 실측 근거는 `posts/_debug/probe/`(리포 루트, gitignore, 집 PC 에만).
   **본문 가독성**(사장님 2026-09-17, 실측 검증 8/8): 본문 16pt·나눔바른고딕, 소제목 19pt 굵게 + 진갈색
   `#823f00` + 앞 빈 줄(다음 문단은 검정 `#000000` 으로 되돌림 — 둘 다 팔레트에 있을 때만 칠한다),
   절마다 핵심 문장 하나 `**굵게**`(프롬프트가 쓰게 하고 `_cap_bold` 가 절당 1개로 자르며, 웹은 `emph`
   필터, 네이버는 `_type_rich` 가 굵게 토글로 친다). 색 복귀에 실패하면 `LAST_WARNINGS` 로 잡 결과
   문장에 '⚠ … 색 번짐 확인'이 붙는다. 실측·검증 스크립트는 프로필 사본(`automation/chrome_profile`
   복사, 캐시 제외)으로 열었다 — 원본 프로필을 열면 일꾼 발행이 막힌다.
3. **예약 발행까지 — 단, 글마다 사람이 누른다** (사장님 2026-09-15, 8/29 의
   '임시저장까지만'을 번복). 글 화면 ⑤에서 사장님이 시각을 보고 [📅 이 시각에
   예약 발행]을 누르면 집 PC 가 글·사진·서식을 넣고 네이버 예약 발행까지 건다
   (`naver_autodraft.reserve_one`, 77c418f 에서 걷어낸 것을 복원). 추천 시각은
   규칙(아침 08:00 — 반응 데이터가 쌓이면 그걸로)으로 미리 채워 두되 언제든 고친다.
   ⚠️ **여전히 금지인 것:** 초안이 생기자마자 스스로 예약을 거는 것, 사람 클릭
   없이 시각을 정해 발행하는 것(옛 `maybe_blog_scheduled` 류). 예약은 반드시
   글 하나에 버튼 하나. '예약 없이 임시저장만'도 그대로 있다.
   웹의 '예약 등록함'은 네이버에서 직접 건 것을 기록하는 칸으로 남겨 둔다.
4. **발행할수록 똑똑해짐** — 사장님 수정·발행본 차이·실제 반응(공감/댓글/순위)이
   다음 글의 프롬프트로 되먹임되는가.

비용 원칙(사장님 확정 2026-08-30): **유료 Claude API 에 의지하지 않는다.**
AI 호출은 무료(Gemini)가 기본이고, 품질·분량은 품질 게이트의 확장 퇴고로
맞춘다(실측: 무료로 82점·2,065자). 유료는 무료가 완전히 죽었을 때의 비상
폴백일 뿐, 기능이 유료를 전제로 설계되면 안 된다.

**단 인스타 릴스는 예외다(사장님 확정 2026-08-30 저녁): 유료 Claude 전용.**
무료 Gemini 한도는 리뷰 답글 몫으로 남긴다. `sns_automation/planner.py` 의
`_ask` 가 `llm.complete(only=("claude",), paid=True, model=INSTA_CLAUDE_MODEL)`
로 부르며, `paid=` 는 그 호출에만 유료를 여는 호출 단위 옵트인이라
답글·블로그의 무료 사다리에는 영향이 없다. 모델 기본 `claude-opus-5`
($5/$25 per 1M tok), `.env` `INSTA_CLAUDE_MODEL` 로 교체 가능.
예상 비용: 릴스 1편당 AI 호출 ≈ $0.2, 주 3편 발행 기준 **월 $5 안팎**.

## 스마트플레이스 페이지의 목표 (사장님 확정 2026-08-30)
`/place` (네이버 스마트플레이스 화면군)를 손댈 때는 항상 이 기준으로 판단한다.

> **"베어글스 송도타임스페이스의 네이버 스마트플레이스를 최적화해서
> → 네이버지도 노출을 올리고 → 그걸로 매출을 올린다."**

세 단계로 쪼개 판단한다. 새 기능은 이 중 **어느 단계를 실제로 움직이는지**
먼저 답할 수 있어야 한다.
1. **최적화** — 플레이스 세팅(메뉴·영업시간·대표번호·톡톡·소식 등)이 채워져
   있는가. 지금 무엇이 비었는지 **사람이 눈으로 찾지 않고 화면에서 바로** 알 수
   있어야 한다.
2. **노출 상승** — 최적화가 실제로 순위·유입에 반영됐는가. 유입 키워드와
   그 변화가 기록으로 남아야 판단할 수 있다.
3. **매출 상승** — 노출 변화가 매장 매출로 이어졌는가. 리포에 이미 있는
   매출 데이터와 나란히 볼 수 있어야 한다.

⚠️ **키워드 상위노출은 목표가 아니라 수단이다.** "송도 베이글" 순위가 올라도
방문·주문으로 안 이어지면 실패로 본다.

⚠️ **범위 밖 — 네이버 리뷰 답글은 이 페이지가 아니다.** 네이버 리뷰 답글은
배달 리뷰(배민·쿠팡) 답글 화면군에 함께 넣어 처리한다(2026-08-30 확정).
이 페이지에서는 리뷰 답글을 다루지 않는다.

세부 전략·근거는 [스마트플레이스-최적화.md](스마트플레이스-최적화.md),
직원 실행용은 `스마트플레이스-직원가이드.html`(웹 `/place` 가 그대로 서빙).
현황 진단은 `python -m scripts.place_audit`.

## 경영 대시보드 페이지의 목표 (사장님 확정 2026-09-03)
`/sales` (경영 대시보드, 사장님 전용)를 손댈 때는 항상 이 기준으로 판단한다.

> **"사장님이 폰으로 열어 5초 안에 '이번 달 장사 어땠나'를 판단한다.
> 결론(판정 문장) → 근거(매출 1만원이 어디로 갔나·게이지) → 상세 순서."**

참고 사양은 `docs/경영대시보드-사양서.md`(2026-08-22 클로드 채팅 산출물),
계산 규칙은 `docs/경영지표-계산규칙.md`. 탭 6개:
1. **진단** — 마지막 *확정* 달의 판정 문장(규칙), 워터폴, 게이지 3개(매출
   목표·손익분기·영업이익), '다음 달 할 일 3개'(규칙·기대효과순), 세부 지표
   12개, 그리고 포스 기준 '이번 달 진행'(매장/배달 누적·목표·3종 비교).
2. **매출** — 맨 위 4칸은 사장님이 보고 싶은 것(2026-09-13): **실제 매출총액**
   (장부 파일 '매출장부' 시트의 `매장_실제_매출총액` + 배달) · **폐기율** ·
   **서비스&직원식대 비율** · **단체주문**(시트 `매장_단체주문`; 건수는
   `매장_단체주문_건수` 행을 시트에 추가하면 읽힌다 — 포스 20만원 이상 결제로
   세면 시트와 안 맞아 안 쓴다). 그 아래 순서(사장님 2026-09-13): **이달 채널별** →
   **주간 매출 추이**(12주, 월요일 시작, 전체/매장/배달 전환, 하루이틀짜리 마지막
   주는 뺌) → **요일별 평균**(8주) · 매장 vs 배달 비중 → 월 요약 → 채널별 월 추이 →
   포스 일별. '전월 대비 증감' 카드는 뺐다(사장님 지시).
   매출장부 시트는 `ledger_store.parse_sales_rows` 가 읽어 `ledger_monthly` 의
   `store_actual_sales·store_waste·store_staff_meal·group_amount·group_count`
   (schema_v15)에 얹는다. 폴더 CSV 길에서는 `*매출장부*.csv` 를 따로 찾는다.
3. **비용** — 변동비/고정비 판정 아래 **비용 항목 4칸을 원 단위로**(사장님 2026-09-15
   "각 항목을 정확히"): 매출액(시트 매출총액) · 인건비(시트 금액) · 임대료(시트엔
   임대료율만 있어 × 매출총액) · 기타 고정비(시트 달별 칸이 비어 고정비 총액 − 인건비 −
   임대료). 칸마다 출처를 적는다 — 시트 값과 계산값을 섞어 보이지 않게
   (`dashboard_page.cost_items`). 그 아래 비율 추이(목표선), 배달 수수료 분해,
   영업이익·플랫폼 실수익.
4. **상품** — TOP 10, 카테고리 비중, 주목 메뉴 추이, 밀 것/지켜볼 것/정리할 것(규칙).
5. **운영** — 요일×시간대 히트맵(`sales_hourly`), 요일별 일평균, 시간대별 매장 vs 배달.
6. **시뮬** — 마감시간·휴무(실제 시간대·요일 매출 × 공헌이익률, 시급·인원 입력),
   배달 실수익 계산기.

원칙(사장님 확정):
- **장부 폴더(사장님 제공 2026-09-03).** 드라이브 `매출:세무관련` 폴더
  https://drive.google.com/drive/folders/1Ud6utvLW__YbELQOae9KW1gRFBkNKMo9
  안의 `장부관리/2026년 N월/` 에 그 달 TOS 엑셀을 올리면 끝. 집 PC 에는
  구글드라이브 데스크톱이 `...\매출 세무관련\장부관리` 로 동기화해 두고
  (드라이브의 `:` 가 로컬에선 공백), `worker/pos_import.py` 가 하루 1회
  새 파일을 찾아 반영한다(경로 바꾸려면 `.env` `MKT_LEDGER_DIR`). 매출
  대시보드·플레이스 페이지가 전부 이 한 곳을 본다.
- **두 원천.** 포스 장부(드라이브 TOS/IMU 엑셀 → `sales_daily`·`sales_hourly`)는
  일 단위, 원가·고정비·정산총액·영업이익은 사장님이 매달 정리하는 **구글 시트
  '베어글스_장부' 요약시트**(→ `ledger_monthly`, schema_v12).
  집 PC 일꾼 `worker/ledger_sheet.py` 가 매일 반영하는데, 읽는 길이 **둘**이고
  되는 쪽을 알아서 쓴다(`load_summary`):
  ① **장부관리 폴더의 CSV**(기본·로그인 불필요) — 시트에서 [파일 → 다운로드 →
     쉼표로 구분된 값(.csv)] 해서 TOS 엑셀 올리는 그 폴더에 넣으면 된다.
     이름에 '장부'만 들어가면 되고 여러 개면 최근 것. 확정/예상은 파일 수정 시각.
  ② **Drive API 로 시트 직접**(자동, `token.json` 필요).
  ②는 2026-09-09~13 막혀 있었다(OAuth 앱 `beargels-sns` 가 조직 전용 → 403
     `org_internal`). **2026-09-13 사장님이 콘솔에서 풀었고** `3_google_login.bat`
     (myeonggu96@gmail.com)로 token.json 이 생겨 지금은 ②가 돈다. 다시 막히면 ①.
     확인법: 대시보드 [🔄 장부 최신화] 결과 문장에 '[구글 시트]'가 찍히면 ②.
  월이 끝나기 전에 적힌 값은 `estimate`
  (화면에 '예상'), 끝난 뒤 갱신되면 `confirmed`. 진단은 확정 달만 쓴다.
  **[🔄 장부 최신화] 버튼**(2026-09-13, 사장님 요청) — 출처 줄 아래. 하루 1회
  자동 반영을 안 기다리고 집 PC 일꾼에게 지금 읽으라고 시킨다: 잡 `ledger_sync`
  (`ledger_store.request_sync` → `worker/agent.py run_ledger_sync_job`, 빠른
  폴링 대상). 화면은 2초마다 `/sales/refresh/<id>` 로 상태를 묻고 끝나면 같은
  달을 다시 그린다. 버튼 경로는 홈 알림함에 안 쌓는다(`notify=False` —
  누를 때마다 같은 알림이 늘면 안 되니까). ⚠️ 새 잡 종류라 **일꾼을 재시작**
  해야 알아듣는다 — 옛 일꾼은 '알 수 없는 잡 종류 … 일꾼 업데이트 필요'로 끝낸다.
  ⚠️ **밀리면 화면이 말한다**(`dashboard_page.ledger_alert`, 2026-09-09 추가).
  지난달이 확정으로 안 들어왔거나, 들어왔어도 자동 반영이 막혀 있으면 진단 탭
  맨 위에 무엇이 없고 무엇을 하면 되는지 띄운다. 원인은 `error_log` 의
  `LedgerSheetError` 를 직접 읽어 사장님 말로 바꿔 보여준다. 이걸 안 하다가
  token.json 이 없어 **6일 연속 조용히 실패**했고(2026-09-04~09), 8월 장부가
  예상치인 채 남아 화면이 7월을 진단하고 있었다 — 사장님이 알려줘서 발견했다.
- **영업이익 = 정산총액 − 매입원가 − 고정비.** 정산총액은 수수료가 이미 빠진
  실입금액이라 매출총액에서 수수료를 또 빼면 이중 차감. 배달 수수료(실질)는
  매출총액 − 정산총액 − 매장 카드수수료(매장매출 × 2.1%)로 **역산**한다 —
  시트의 '배달 수수료(+광고)' 행은 정산총액과 안 맞아 참고값으로만 둔다.
- **크롤러 잠정치는 섞지 않는다.** 포스 장부 없는 날은 빈 채로 두고, 월초라
  이번 달 장부가 한 줄도 없으면 장부 있는 마지막 달로 대신 열고 밝힌다.
- **판정·할 일은 규칙이다(AI 비용 0).** `service/dashboard_page.py` 의
  `verdict / actions / product_buckets`. 규칙을 바꾸면 `tests/test_ledger.py` 도.
- **사장님 전용 — 열 때마다 비밀번호(사장님 지시 2026-09-08).** 사이드바 '📊 경영
  대시보드' 메뉴는 누구에게나 보이고, 누르면 비밀번호 화면(`sales_lock.html`)이
  먼저 뜬다. 비밀번호는 `OWNER_KEY` 환경변수, 없으면 `menu_settings.owner_key`
  (지금 값은 사장님이 정한 6자리, 리포에는 적지 않는다).
  ⚠️ **기억하는 쿠키를 두지 않는다** — 폰을 잠깐 빌려주거나 직원이 열어도 그냥
  열리면 안 된다(2026-09-07 의 1년 쿠키 방식을 사장님이 되돌렸다). 그래서
  `/sales` 는 **언제나** 잠금 화면이고, 맞으면 그 POST 응답으로 대시보드를 바로
  그린다(리다이렉트·쿠키 없음, `Cache-Control: no-store`). 화면 안 월 이동만
  30분짜리 서명 토큰(`_nav_token`, 저장 안 함)을 폼에 실어 잇고, 목표 저장 fetch
  도 그 토큰을 보낸다(없거나 낡으면 401). 옛 `/sales?k=<키>` 바로열기는 없앴다 —
  북마크가 곧 무잠금이 되기 때문. 틀리면 같은 화면에 안내만(0.6초 지연).
- **폰이 주 화면.** 차트는 Chart.js(cdnjs) — 이 화면만 예외로 외부 라이브러리를
  쓴다(참고 사양과 같은 모양을 빠르게 내기 위해). 색은 CSS 변수만.
- 매출 목표는 화면 입력(`menu_settings.sales_goals`)이 우선, 없으면 시트 '목표'
  열(`ledger_targets`).

⚠️ 장부 양식 주의: 2026-08 TOS 부터 '결제 합계' 시트의 헤더와 매입사 이름이
한 줄에 있다. 예전 파서는 배달 채널을 못 잡아 매장이 총액으로 부풀려졌다
(2026-09-03 발견·수정). 새 장부가 이상하면 `worker/pos_import.py` 의
`_parse_tos_daily` 부터 본다. 시간대(`sales_hourly`)는 TOS '결제 상세내역'의
취소(음수 행)를 부호째 합산해야 '결제 합계'와 원 단위로 맞는다.

## 인스타그램 파이프라인의 목표 (사장님 확정 2026-09-17 — 블로그와 별도)
`/instagram` (사이드바 '🎬 인스타그램')과 `sns_automation/*` 을 손댈 때는 항상 이 기준으로
판단한다. **인스타와 네이버 블로그는 워크플로가 다르다** — 기획·진행·성과를 서로 얽지 않는다.

> 인스타 시장조사 → 주제 제안 → 촬영 → 릴스(·게시글) 제작 → 사람이 발행 → 인스타 성과 → 다음 기획

원칙(사장님 확정):
- **기획은 철저히 인스타그램 기준으로 판단한다.** 근거의 우선순위는 ①내 계정에서 실제로
  먹힌 훅·소재(좋아요·댓글) ②동네·카테고리 해시태그 인기글의 문법 ③릴스 편집 트렌드
  ④계절·날짜 ⑤지금 잘 팔리는 메뉴 — **⑤는 주제 기준이 아니라 '어떤 메뉴로 찍을까'의 보조**.
  네이버 유입 검색어·네이버 키워드 실측·블로그 각도는 인스타 프롬프트에 넣지 않는다
  (`planner._ideas_system`, 2026-09-17 에 뺐다 — 되살리지 말 것).
  ⚠️ **모르는 숫자는 프롬프트에 쓰지 않는다.** 해시태그 웹 수집은 좋아요·릴스 여부를 못
  가져온다 — 예전엔 '♥0·릴스 비중 0%'로 찍혀 "이 시장은 릴스를 안 쓴다"는 거짓이 들어갔다.
- **화면은 카드 3개**(2026-09-07 정리): 📥 올린 소재로 릴스 만들기(이번 주 소재만 펼침, 찍을
  것·지난 소재는 접기) → ✅ 완성된 릴스(미발행만 펼침) → 💡 뭘 찍을까요(제안 + [＋ 내가 정한
  주제] + [이건 안 할래요] + 📎 레퍼런스). 맨 위 '할 일 칩'이 다음 행동을 가리킨다. 기능을
  더할 땐 **사장님 손이 몇 번 가는지부터 센다** — 사람은 입구(촬영)와 출구(발행)만.
- **제안은 자동으로 돌지 않는다**(사장님: "주기적 x / 내가 원할 때 버튼으로"). 주간
  시장조사(`maybe_market_scan`, 내 계정 API + 해시태그 웹)는 조사만 하고, 제안은 [💡 새
  아이디어 주세요](잡 `reel_ideas`)를 눌렀을 때만 짠다. 누를 때마다 쌓이므로 프롬프트에
  '이미 제안해 둔 주제'·'접은 주제'를 넣어 겹치지 않게 한다(`briefs.as_prompt_context`).
  [＋ 내가 정한 주제] → 잡 `reel_ref` 의 `topic` → `planner.plan_from_topic`(주제는 안 바꾸고
  훅·샷만 붙인다). [이건 안 할래요] → 잡 `brief_dismiss` → `briefs.dismiss`(지우지 않고
  표시 — 지우면 같은 주제를 또 들고 온다). 브리프 원본은 집 PC 것이라 쓰기는 전부 잡으로.
- **브리프는 인스타 전용이다**(`sns_automation/briefs.py`). 주제 하나에 번호 하나, 상태는
  제안 → 촬영중 → 소재도착 → 제작중 → 발행 → 종료(앞으로만). 저장은 집 PC `data/briefs.json`
  원본, 버킷 `state/briefs.json` 웹 사본. `blog` 필드는 데이터 호환용으로 남아 있을 뿐 새로
  채우지 않는다. 옛 '두 채널 관통' 설계(2026-09-04)는 사장님 지시로 2026-09-17 에 풀었다.
  ⚠️ `briefs.push()` 는 원본 파일이 없는 자리에서 부르면 올리지 않는다 — 웹 사본이 0개로
  덮인 실사고(2026-09-17) 때문.
- **촬영 리듬은 주 3회 기획 촬영.** [📸 이거 찍을게요] → 잡 `reel_shoot` → 집 PC 가 소재
  폴더와 촬영가이드.txt 를 미리 만든다. 올리면 10분 안에 **입고 검수**(`intake_qc.py`,
  `maybe_intake_qc`)가 못 쓰는 컷(짧음·어두움·흔들림·무음)과 모자란 컷 수를 알려준다.
  ⚠️ 어느 샷이 빠졌는지는 알 수 없다 — '컷이 모자라다'로만 말한다.
- **발행은 사람이, 발행 사실은 시스템이 반드시 안다.** 완성본 카드 [📤 인스타에 올렸어요]
  → 잡 `reel_published`. 안 눌러도 집 PC 가 **하루 한 번**(2026-09-17, 예전 6시간) 내 계정
  게시물을 읽어 캡션으로 맞추고 좋아요·댓글을 훅 라이브러리에 적는다(`publish_sync.py`,
  `maybe_reel_sync`). 성과는 다음 제안 프롬프트로 되먹임된다.
- **AI 사진·영상: 보정 O, 제품 실물 생성 X.** 곰 캐릭터·배경 그래픽·텍스트 카드처럼 실물이
  아님이 자명한 것만 생성.
- **인스타 시장조사는 조사 전용 계정으로 분리한다**(매장 계정 보호). 미구현 — 전용 크롬
  프로필에 조사 계정으로 로그인하면 된다(`market_scan.scan_web`).
- 소재 사용 원장(`worker/media_ledger`)에 릴스가 쓴 컷을 올리던 연결은 끊었다 — 블로그
  사진은 사장님이 폰에서 직접 올려 원장을 볼 일이 없다. 쓴 컷은 project.json 에만 남는다.

없앤 것(2026-09-17): 통합 '콘텐츠 기획' 화면 `/mkt`(`service/plan_page.py`·`plan.html`)과 홈의
콘텐츠 기획 카드. 쓸 만한 버튼([＋ 내가 정한 주제]·[이건 안 할래요])은 `/instagram` 💡 카드로
옮겼다. 홈 '이번 주 인스타'는 남는다(브리프 기준). 되살리려면 git 이력(같은 날 오전 ccb285f).
`database/mkt_store.py` 는 이름만 옛것 — **매출 읽기·쓰기만** 남았다(대시보드·플레이스가 쓴다).

논의 중(아직 안 만듦): ✂️ 컷 확인(렌더 전에 컷을 이어보기로 보고 그 컷만 수정 — 기본 흐름을
「장면 선택 → 컷 확인 → 렌더 1번」으로) · 🖼 게시글(카드뉴스) 생성기(브랜드 템플릿 고정).
남은 것: 조사 전용 계정 분리 · `data/*.json` 백업 · 인사이트 권한(`instagram_manage_insights`).

## 네이버 조사 도구 (블로그 키워드 전용)
아래는 **블로그** 키워드를 고르는 도구다. 인스타 기획에는 쓰지 않는다(2026-09-17).
- **근거의 우선순위: ①우리 유입 검색어 ②우리 매출 ③네이버 경쟁**(시장조사 검토 2026-09-04).
  `sns_automation/first_party.py` 가 스마트플레이스 유입 검색어(`menu_settings.place_keywords`,
  주 1회 자동 수집)와 상품별 실매출(`product_sales_daily`)을 프롬프트에 넣는다.
  **조사 씨앗도 유입 검색어에서 뽑는다**(`naver_search.merged_seeds`) — 코드에 박아둔 씨앗만
  쓰면 조사 대상이 영원히 고정된다(실측: 유입 50개 중 12개만 닿았고 '타임스페이스' 계열 30회가
  통째로 빠져 있었다).
  ⚠️ 자동완성에 없어도 **유입이 증명된 말은 수요가 있는 것으로 본다**(`known_demand`).
  ⚠️ 유입 검색어는 붙여쓴 형태라 자동완성이 주는 표준 표기로 맞춰서 잰다(`naver_search.canonical`).
- **검색량은 네이버 검색광고 키워드도구**(`sns_automation/keyword_volume.py`, 2026-09-05).
  무료이고 광고를 집행하지 않아도 된다(실측: 잔액 0원 계정에서 200). 키는 `.env` 의
  `NAVER_AD_API_KEY`/`NAVER_AD_SECRET_KEY`/`NAVER_AD_CUSTOMER_ID`, 절차는
  `docs/naver_searchad_setup.md`. 키가 없으면 경쟁만으로 판정한다 — 기능이 죽지 않는다.
  ⚠️ 서명은 `타임스탬프.메서드.**경로**`(쿼리스트링 넣으면 403). ⚠️ 응답은 문자열이고 10 미만은
  `< 10`(중앙값 5로 읽는다). ⚠️ 호출 제한이 빡빡하다 — 한 번에 5개씩.
- **순서는 '기회 점수'(검색량 × 빈자리)**(`naver_search.opportunity`). 등급은 넣고 뺄 때만.
  월 100회 미만은 `tiny` 로 걸러낸다. 단 **유입이 증명된 말은 예외**(도구 월 15회 vs 플레이스
  유입 주 8회 — 우리 측정이 도구 추정보다 위).
- **블로그 키워드는 네이버 실측 위에서 고른다**(`naver_search.py`, 주 1회 `maybe_naver_research`).
  자동완성=수요 여부, 블로그탭 상위 30의 정면 경쟁글 수=경쟁, 키워드도구=수요의 크기.
  판정은 규칙(AI 비용 0): green / yellow / mine / tiny / red. **경쟁 가게 상호와 다른 동네
  송도(부산·포항)는 후보에서 뺀다**(새 상호는 `.env` `NAVER_EXCLUDE`).
- 당근·플레이스 소식 채널은 두 루프가 한 바퀴 돈 뒤에 넓힌다.

## 실행 위치
- **텔레그램 봇은 사용하지 않는다** (2026-08-11 결정). 실행은 **웹 파이프라인
  (`5_웹파이프라인.bat`)** 을 집 PC에서 돌리는 방식.
  봇 계열 코드(main.py·4_run_bot.bat·telegram_bot.py·scheduler/·supervisor)는
  퇴역 뒤 한 달 넘게 손대지 않아 **2026-08-28 에 지웠다** — 되살리려면 git 이력.
- 에이전트 구조·역할 분담은 `docs/agents_design.md` 참고.
- 클라우드 세션은 **코드 개발·수정** 용도. 봇을 24시간 돌리지 못한다(컨테이너가 임시).
- PC에서 새 코드를 pull 받으면, 돌던 봇을 멈췄다가 다시 실행해야 새 코드가 적용된다.
